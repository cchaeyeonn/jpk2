<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "ezen.dev.spring.admin">

	<select id="getMemberList" resultType="memberVo">
		select * from member
	</select>
	
	<insert id="productAdd" parameterType="productVo">
		insert into fm_product(p_name, p_filename, p_price, p_unit, p_weight, p_delivery, p_package, p_allergy, p_limitdate, p_type, p_tag) 
		 values(#{p_name}, #{p_filename}, #{p_price}, #{p_unit}, #{p_weight}, #{p_delivery}, #{p_package}, #{p_allergy}, #{p_limitdate}, #{p_type}, #{p_tag})
	</insert>

</mapper>