<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "ezen.dev.spring.product">

<!-- 	<select id="getProductInfo" parameterType="string" resultType="productVo">
		select p_name,p_filename,p_price,p_unit,p_weight,p_delivery,p_package,p_allergy,p_limitdate from fm_product where pidx=#{pidx}
		
		
	</select>
 -->
</mapper>